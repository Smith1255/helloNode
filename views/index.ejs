<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  May Node and Express be with you.

  <ul class="hello">
  <% for(var i=0; i<hello.length; i++) {%>
    <li class="quote">
      <span><%= hello[i].name %></span>
      <span><%= hello[i].quote %></span>
    </li>
  <% } %>
  </ul>

  <form action="/" method="POST">
    <input type="text" placeholder="name" name="name">
    <input type="text" placeholder="quote" name="quote">
    <button type="submit">Submit</button>
  </form>
  
  <div>
	<h2>Get rid of imposters</h2>
        <button id="update">Seek out phonies</button>
        </div>
<script src="main.js"></script>
</body>
</html>
